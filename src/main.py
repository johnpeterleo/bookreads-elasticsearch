#
#   To-Do:
#
#   User gives input (previous books (both ratings, genres) + some input such as genres (or maybe minimum rating))
#   build query
#   then query Elasticsearch (using search.py)
#   return recommendations (using recommendations.py)